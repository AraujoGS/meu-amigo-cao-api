import { badRequest, notFound, preconditionFailed, internalServerError, unauthorized } from './components/'

export default {
  badRequest,
  notFound,
  preconditionFailed,
  internalServerError,
  unauthorized
}
